export default interface User {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  birthday: string;
  country: string;
  picSrc: string;
  recieveNotif: string;
  contestToData: boolean;
}
